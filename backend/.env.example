# Supabase / PostgreSQL (optional for Phase 1 — runs without DB using in-memory cache)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Upstash Redis (optional — falls back to in-memory cache)
REDIS_URL=

# Server
HOST=0.0.0.0
PORT=8000
